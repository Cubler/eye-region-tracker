import web
import runModel

urls = (
    '/', 'index'
)

class index:
    def GET(self):
        output = runModel.runModel()
        return runModel

if __name__ == "__main__":
    app = web.application(urls, globals())
    app.run()

